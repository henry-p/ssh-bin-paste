{
  "name": "ssh-bin-paste",
  "version": "0.1.0",
  "description": "Paste local clipboard images into remote terminal agents over SSH.",
  "type": "module",
  "bin": {
    "ssh-bin-paste": "dist/cli.js"
  },
  "files": [
    "dist",
    "native",
    "remote"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.6"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}

